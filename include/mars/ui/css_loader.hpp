#pragma once

#include <string_view>

namespace mars::ui {
    template <typename T>
    T parse(const std::string_view& _view) {
    }

    template <typename T>
    struct css_loader {
    };
} // namespace mars::ui